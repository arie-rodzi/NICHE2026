# NICHE 2026 Registration System

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Default admin:
- username: `admin`
- password: `NICHE2026admin`

For Streamlit Cloud, set secrets:

```toml
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="your_new_password"
```
